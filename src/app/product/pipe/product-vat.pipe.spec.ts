import { ProductVatPipe } from './product-vat.pipe';

describe('ProductVatPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductVatPipe();
    expect(pipe).toBeTruthy();
  });
});
